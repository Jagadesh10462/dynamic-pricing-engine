services:
  - type: web
    name: dynamic-pricing-backend
    runtime: java
    plan: free
    rootDir: backend/dynamic-pricing-engine
    buildCommand: "./mvnw clean package -DskipTests"
    startCommand: "java -jar target/*.jar"
    envVars:
      - key: JAVA_VERSION
        value: "17"